{
  "name": "shift-tracker",
  "version": "1.0.0",
  "description": "Shift sales tracker — Express + JSON file backend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
